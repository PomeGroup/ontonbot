/*
Right now it just returns the same as SnapshotComing component but it may change that's why it has it's own component 
*/

import { SnapshotComing } from "./SnapshotComing";

export const SnapshotClaimPoints = () => {
  return <SnapshotComing />;
};
